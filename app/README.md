# Streamizdat Web

Next.js frontend for the Streamizdat platform.

## Routes
- `/` Home
- `/live` Live player (HLS)

Configure the HLS playlist URL via env `NEXT_PUBLIC_HLS_URL` else defaults to `http://localhost:8080/live/eroxl.m3u8`.
